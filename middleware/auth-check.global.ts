export default defineNuxtRouteMiddleware((to) => {
  // const store = useMainStore()
  // const needsAuth = /^\/dashboard/.test(to.path ?? '')

  // const currentUser = toRaw(Object.assign({}, store.$state).currentUser)
  // if (!currentUser && needsAuth) {
  //   if (to.redirectedFrom) {
  //     // pass
  //     return to.redirectedFrom
  //   } else {
  //     return '/'
  //   }
  // }
})
